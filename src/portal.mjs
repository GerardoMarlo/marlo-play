import { sections } from './catalog.mjs';
const escape = value=>String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function portal(css){return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="Games, experiments and other things by MARLO."><link rel="canonical" href="https://marlo.games/"><title>MARLO — Games & other things</title><style>${css}</style></head><body><div class="shell"><header><a class="brand" href="/">MARLO<i>✳</i></a><span class="header-note">INDEPENDENT PLAY & CURIOSITY</span></header><main><div class="hero"><span class="eyebrow">A SMALL COLLECTION. AN OPEN INVITATION.</span><h1>MARLO<span>.</span></h1><p>Games to get lost in.<br>Other things to get curious about.</p></div>${sections.map((s,i)=>`<section class="section"><h2 style="margin:0"><button class="banner" id="toggle-${s.id}" aria-expanded="${s.open}" aria-controls="panel-${s.id}"><span class="num">0${i+1}</span><strong>${escape(s.title)}</strong><span class="badge">${s.entries.length.toString().padStart(2,'0')}</span><span class="chevron" aria-hidden="true">⌄</span></button></h2><div class="panel ${s.open?'open':''}" id="panel-${s.id}" role="region" aria-labelledby="toggle-${s.id}" ${s.open?'':'inert'}><div>${s.entries.length?`<div class="entries">${s.entries.map(e=>`<a class="game" href="${escape(e.url??`https://play.marlo.games/${encodeURIComponent(e.slug)}`)}"><div class="emblem" aria-hidden="true">P</div><div><span class="tag">${escape(e.tag??e.status)}</span><h3>${escape(e.title)}</h3><p>${escape(e.description)}</p></div><div class="play">Play game <span aria-hidden="true">↗</span></div></a>`).join('')}</div>`:`<p class="empty">${escape(s.empty)}<small>Tools, experiments and whatever comes next.</small></p>`}</div></div></section>`).join('')}</main><footer><span class="footer-mark">MARLO · MADE TO BE EXPLORED</span><span>PLAY. DISCOVER. COME BACK.</span></footer></div><script>document.querySelectorAll('.banner').forEach(button=>button.addEventListener('click',()=>{const open=button.getAttribute('aria-expanded')!=='true';button.setAttribute('aria-expanded',String(open));const panel=document.getElementById(button.getAttribute('aria-controls'));panel.classList.toggle('open',open);panel.inert=!open;}));</script></body></html>`;}
